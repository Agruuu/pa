<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.orhon.pa.modules.opa.dao.OpaMenuDao">
    
	<sql id="opaMenuColumns">
		a.id AS "id",
		a.pid AS "pid",
		a.name AS "name"
	</sql>
	
	<sql id="opaMenuJoins">
	</sql>
    
	<select id="get" resultType="OpaMenu">
		SELECT 
			<include refid="opaMenuColumns"/>
		FROM opa_menu a
		<include refid="opaMenuJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="OpaMenu">
		SELECT 
			<include refid="opaMenuColumns"/>
		FROM opa_menu a
		<include refid="opaMenuJoins"/>
		<where>
			
			<if test="name != null and name != ''">
				AND a.name LIKE 
					<if test="dbName == 'oracle'">'%'||#{name}||'%'</if>
					<if test="dbName == 'mssql'">'%'+#{name}+'%'</if>
					<if test="dbName == 'mysql'">concat('%',#{name},'%')</if>
			</if>
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="OpaMenu">
		SELECT 
			<include refid="opaMenuColumns"/>
		FROM opa_menu a
		<include refid="opaMenuJoins"/>
		<where>
			
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO opa_menu(
			id,
			pid,
			name
		) VALUES (
			#{id},
			#{pid},
			#{name}
		)
	</insert>
	
	<update id="update">
		UPDATE opa_menu SET 	
			pid = #{pid},
			name = #{name}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		DELETE FROM opa_menu
		WHERE id = #{id}
	</update>
	
</mapper>